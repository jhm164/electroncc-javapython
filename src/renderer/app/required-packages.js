/* Please do not use strict mode here */
/* Also make sure to properly order the JS libraries (functions are not hoisted in JS like variables) */
/* As I'm not arranging the files based on the dependencies */
/* Just grouping client's js and server's js in different groups */

var request = require("request");
var fs = require("fs");
var path = require("path"); 
var os = require("os");